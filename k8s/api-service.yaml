apiVersion: v1
kind: Service
metadata:
  name: items-api-service
  namespace: items-app
spec:
  selector:
    app: items-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
  type: NodePort